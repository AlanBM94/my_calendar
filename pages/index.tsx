import React from 'react';
import { ContentContainer, Layout } from '@/components/index';
import { ThemeProvider } from '@mui/material';

const Home: React.FC = () => {
  return (
    <Layout>
      <ContentContainer />
    </Layout>
  );
};

export default Home;
